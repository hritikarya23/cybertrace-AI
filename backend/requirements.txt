# ---- Phase 1: Project foundation ----
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
email-validator>=2.2,<3.0
pydantic-settings==2.5.2
python-dotenv==1.0.1
httpx==0.27.2
pytest==8.3.3

# ---- Added in later phases (kept here, commented, so the final
#      requirements.txt file's history is easy to follow phase by phase) ----
 sqlalchemy==2.0.35          # Phase 2 - database
 alembic==1.13.2             # Phase 2 - migrations
 psycopg[binary]>=3.2.10,<3.4 # Phase 2 - PostgreSQL driver
 python-jose[cryptography]==3.3.0   # Phase 3 - JWT
 passlib[bcrypt]==1.7.4      # Phase 3 - password hashing
 python-multipart==0.0.9     # Phase 4 - file uploads (.eml)
 pytest-asyncio==0.24.0       # testing async routes (added when we have real async I/O to test)
 PyJWT==2.10.1
 pwdlib[argon2]
asyncpg==0.30.0
reportlab>=4.2,<5.0
